# สร้าง Microservices อย่างฉลาด: กลยุทธ์ Hybrid Database ที่สมดุลระหวางประสิทธิภาพและต้นทุน

ในโลกของ Microservices หลักการ `Database-per-Service` คือสิ่งที่เราทุกคนต่างฝันถึง มันให้ความอิสระ ความสามารถในการขยายตัว และการแยกส่วนที่ชัดเจน แต่ในความเป็นจริง เมื่อเริ่มต้นโปรเจคใหม่ๆ การจัดหา Database Instance ให้ทุก Service นั้นมักจะเป็นเรื่องที่หนักอกหนักใจเรื่องต้นทุน

แล้วเราจะทำอย่างไรดี? เราจะเริ่มต้นระบบของเราได้อย่างไรโดยที่ไม่ต้องเสียเงินลงทุนมหาศาล แต่ก็ยังคงไว้ซึ่งความสามารถในการเติบโตและแยกส่วนที่สำคัญออกไปได้ในอนาคต

บทความนี้จะพาคุณไปเจาะลึกกลยุทธ์ที่เราเรียกว่า **"Hybrid Database Strategy"** ซึ่งเป็นแนวทางปฏิบัติที่ชาญฉลาด ช่วยให้คุณสร้างระบบ Microservices ที่แข็งแกร่งได้อย่างยั่งยืน

---

## 1. ทำความเข้าใจโครงสร้างระบบของเราก่อน

ก่อนจะพูดถึงฐานข้อมูล เราต้องมีภาษาร่วมกันก่อน ให้จินตนาการระบบของเราเป็นเมืองหนึ่งเมือง:

*   **Platform (แพลตฟอร์ม):** เหมือนกับ "เมือง" ขนาดใหญ่ เช่น `E-Commerce Platform`, `User Management Platform`
*   **System (ระบบ):** เหมือนกับ "เขต" หรือ "ย่าน" ภายในเมือง เช่น `Product System`, `Order System`
*   **Feature (ฟีเจอร์):** **คือ Application หรือ Program 1 ตัวที่สมบูรณ์** ซึ่งสามารถทำหน้าที่ได้เป็นอิสระ คล้ายกับ "อาคาร" แต่ละหลังในย่านนั้นๆ
    *   ภายในแต่ละ Feature ประกอบด้วย **UI (Frontend)** และ **Service (Backend)**
*   **Microservice (ไมโครเซอร์วิส):** คือ **ส่วน Service (Backend) ของ Feature** ซึ่งเป็น **หน่วยที่เล็กที่สุดที่เราสามารถ Deploy ได้ (Smallest Deployment Unit)**

> **กฎเหล็ก:** **1 Feature = 1 Application (UI + Service)** และ **1 Service = 1 Microservice**

---

## 2. ปัญหาใหญ่: ฐานข้อมูล "One Size" ไม่เคยพอดีกับใคร

หากเราใช้กลยุทธ์เดียวสำหรับทุก Feature มันจะเกิดปัญหาได้ง่ายๆ

*   **ถ้าใช้แชร์ฐานข้อมูลทั้งหมด:** เราจะประหยัดต้นทุน แต่จะเผชิญกับปัญหา **"Noisy Neighbor"** เมื่อ Service ของ Feature หนึ่ง (เช่นระบบชำระเงิน) ทำงานหนัก มันจะทำให้ Service ของ Feature อื่นๆ ทั้งหมด (เช่นระบบแสดงความคิดเห็น) ทำงานช้าลงด้วย
*   **ถ้าใช้ Database-per-Service ทั้งหมด:** เราจะได้ประสิทธิภาพสูงสุด แต่ต้นทุนในช่วงเริ่มต้นจะสูงเกินไป และอาจเป็นการเสียเปรียบสำหรับ Feature ที่ไม่ได้ใช้งานบ่อย

ดังนั้น เราต้องการแนวทางที่ยืดหยุ่นกว่านี้

---

## 3. ทางออก: กลยุทธ์ Hybrid Database แบบ T-Shirt Sizing

แทนที่จะใช้ขนาดเดียวกับทุกคน เรามาจำแนกขนาดของแต่ละ Feature (และ Service ของมัน) และเลือกใช้กลยุทธ์ฐานข้อมูลที่เหมาะสมกับมันกันดีกว่า

| ขนาด | ลักษณะ | ตัวอย่าง Feature |
| :--- | :--- | :--- |
| **Small (S)** | ไม่ใช่หัวใจหลัก, Workload ต่ำ, ทน Downtime ได้ | ระบบรีวิวสินค้า, รายการโปรด |
| **Medium (M)** | ส่วนหนึ่งของการทำงานหลัก, Workload ปานกลางถึงสูง | ระบบสินค้าและคลัง, ตะกร้าสินค้า |
| **Large (L)** | **สำคัญที่สุดต่อธุรกิจ**, Workload สูงมาก | ระบบชำระเงิน, ระบบสั่งซื้อหลัก |

---

## 4. เทียบโครงสร้างกับฐานข้อมูล: ใครนั่งที่ไหน?

นี่คือหัวใจของกลยุทธ์ของเรา การจัดวาง Service ของ Feature แต่ละขนาดลงในฐานข้อมูลที่เหมาะสม

| โครงสร้าง | ขนาด Feature | **กลยุทธ์ฐานข้อมูล** | **ระดับฐานข้อมูล** | ตัวอย่างชื่อ |
| :--- | :--- | :--- | :--- | :--- |
| **Feature** | **Large (L)** | Database per Feature | **Database Instance** ของตัวเอง | `db-payment-service` |
| **Feature** | **Medium (M)** | Database per Platform + Schema per Feature | **Schema** ใน Platform DB | `db-ecommerce-platform` -> `schema-shopping-cart` |
| **Feature** | **Small (S)** | Database per Domain + Schema per Feature | **Schema** ใน Domain DB | `db-ecommerce-aux` -> `schema-reviews` |

**คำอธิบาย:**
*   **L-Feature:** Service ของมันได้รับการพิจารณาให้มีบ้านหลังใหญ่ (`Database Instance`) ของตัวเองทันที เพราะความสำคััญและความต้องการทรัพยากรสูง
*   **M-Feature:** Service ของมันอาศัยอยู่ในอาคารใหญ่ (`Database Instance` ของ Platform) แต่มีห้องของตัวเอง (`Schema`) ที่ชัดเจน
*   **S-Feature:** Service ของมันอาศัยรวมกันในอพาร์ตเมนต์ (`Database Instance` ของ Domain) แต่ก็มีห้องส่วนตัว (`Schema`) ของตัวเองเช่นกัน

---

## 5. วิเคราะห์ข้อดีและข้อเสีย

### ข้อดี (Pros)
*   **💸 Cost-Optimized:** ใช้ทรัพยากรอย่างมีประสิทธิภาพ จ่ายเพิ่มเฉพาะส่วนที่จำเป็นจริงๆ
*   **🚀 Highly Scalable:** สามารถขยายตัวเฉพาะ Service ของ Feature ที่สำคัญได้อย่างอิสระ
*   **🧱 Clear Boundaries:** การแบ่งแยกที่ชัดเจนทำให้ทีมเข้าใจความรับผิดชอบและลดความซับซ้อน
*   **🔮 Future-Proof:** ระบบถูกออกแบบมาให้พร้อมเปลี่ยนแปลงและเติบโตได้

### ข้อเสียและความเสี่ยง (Cons & Risks)
*   **🤔 Initial Complexity:** การจำแนกขนาด Feature และเลือกกลยุทธ์ต้องการความเข้าใจในช่วงแรก
*   **🧘 Requires Discipline:** ทีมพัฒนาต้องมีวินัยในการปฏิบัติตามข้อกำหนดอย่างเคร่งครัด
*   **📊 Monitoring Overhead:** ต้องการระบบเฝ้าระวังที่ดีและละเอียดเพื่อจับสัญญาณการเปลี่ยนแปลง

---

## 6. แนวทางการนำไปใช้จริง: สร้างเกราะให้ระบบ

กลยุทธ์นี้จะทำงานได้จริงต่อเมื่อเราสร้าง "เกราะ" คุ้มครองมันด้วยกฎและเครื่องมือ

### 1. บังคับใช้ Service Boundary
*   **ห้าม JOIN ข้าม Feature:** การเชื่อมต่อข้อมูลระหว่าง Service ของ Feature ต่างกันต้องทำผ่าน API เท่านั้น
*   **แยก Project Structure:** แต่ละ Feature (Application) ต้องมี Project แยกหมด พร้อม `dataAccess` layer ของ Service ตัวเอง
*   **Dedicated DB User:** แต่ละ Service ต้องใช้ Database User ของตัวเอง และมีสิทธิ์เฉพาะในเขตแดนที่กำหนด

### 2. ใช้เครื่องมือให้เป็นประโยชน์
*   **Prisma `@@map`:** ใช้สำหรับบังคับการตั้งชื่อตารางผ่าน Configuration เพื่อลดข้อผิดพลาดจากมนุษย์
*   **Liquibase:** ทุกการเปลี่ยนแปลง Schema ต้องผ่าน Migration Script ที่ถูก Version Control

### 3. วางแผนการ Migration (Evolution Path)
เมื่อ Service ของ Feature เติบโตขึ้น เราต้องมีแผนที่จะย้ายบ้านให้มัน:
*   **S -> M:** ย้ายจากอพาร์ตเมนต์ (`db-domain-aux`) ไปอยู่อาคารใหญ่ (`db-platform`)
*   **M -> L:** ย้ายจากห้องในอาคารใหญ่ออกไปสร้างบ้านหลังใหญ่ของตัวเอง (`db-feature`)

---

## 7. ภาพรวมสถาปัตยกรรม

```mermaid
graph TD
    subgraph "E-Commerce Platform"
        subgraph "L-Feature: Payment Service"
            L_DB[Database Instance: db-payment-service]
            L_Micro[Microservice: Payment Service]
            L_DB -- Owns --> L_Micro
        end

        subgraph "M-Feature: Product System"
            M_DB[Database Instance: db-ecommerce-platform]
            subgraph "M-Features"
                M_Schema1[Schema: schema-product-catalog]
                M_Schema2[Schema: schema-shopping-cart]
            end
            M_DB -- Contains --> M_Schema1
            M_DB -- Contains --> M_Schema2
            M_Micro1[Microservice: Product Catalog]
            M_Micro2[Microservice: Shopping Cart]
            M_Schema1 -- Owns --> M_Micro1
            M_Schema2 -- Owns --> M_Micro2
        end

        subgraph "S-Feature: Auxiliary Domain"
            S_DB[Database Instance: db-ecommerce-aux]
            subgraph "S-Features"
                S_Schema1[Schema: schema-reviews]
                S_Schema2[Schema: schema-wishlist]
            end
            S_DB -- Contains --> S_Schema1
            S_DB -- Contains --> S_Schema2
            S_Micro1[Microservice: Reviews]
            S_Micro2[Microservice: Wishlist]
            S_Schema1 -- Owns --> S_Micro1
            S_Schema2 -- Owns --> S_Micro2
        end
    end

    style L_DB fill:#ff9999,stroke:#333,stroke-width:2px
    style M_DB fill:#99ccff,stroke:#333,stroke-width:2px
    style S_DB fill:#99ff99,stroke:#333,stroke-width:2px